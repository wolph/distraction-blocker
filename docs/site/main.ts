// Placeholder entry point. Vite drops an empty module and its script tag from the build, so this
// keeps the landing page's own entry alive until a later task in this plan replaces it in full.
console.info('Focus Lock demo: placeholder script loaded.');
