// Placeholder entry point. Vite drops an empty module and its script tag from the build, so this
// keeps the fake tab document's own entry alive until Task 5 replaces it in full.
console.info('Focus Lock demo: placeholder tab script loaded.');
