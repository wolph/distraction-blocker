import { defineManifest } from '@crxjs/vite-plugin';

/** Placeholder so the tooling scaffold typechecks. The full manifest lands in the next task. */
export default defineManifest({
  manifest_version: 3,
  name: 'Focus Lock',
  version: '0.1.0',
});
